import {} from '@testing-library/vue';

// import Login from './Login.vue';

describe('Login', () => {
  it.todo('prevents submitting form when username is empty');
  it.todo('emits event once login is successful');
  it.todo('shows an error message once login is failed');
});
